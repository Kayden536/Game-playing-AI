# Game-playing-AI

Please install UV, and use uv venv in the terminal then use uv pip install -r requirements.txt to get what you need in order to run the code.
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
winget install --id=astral-sh.uv -e
check with uv --version or uv venv
